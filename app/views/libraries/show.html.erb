<h2>Books Index For <%= @library.library_name %></h2>

<ul class="list-group">
  <% @books.each do |book| %>
    <li class="list-group-item tab-pane"><%= link_to book.title, book_path(book), :class => "" %></li>
  <% end %>
</ul>
